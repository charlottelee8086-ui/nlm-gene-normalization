# Original experiment scripts

This directory keeps copies of the original scripts used during the project.

The files are preserved mainly for traceability. Many of them were written while experiments were being developed, so there are repeated scripts, intermediate versions, and experiment-specific file paths.

For a cleaner view of the project, use the code under `src/` and the experiment descriptions under `experiments/`.

This directory contains some historical codes used during the experimental reasoning process.
